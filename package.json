{
  "name": "hide-youtube-duration",
  "version": "1.0.0",
  "description": "Chrome extension to hide video duration on YouTube",
  "type": "module",
  "scripts": {
    "format": "prettier --write 'src/**/*.{css,html,js}'",
    "build": "rm -rf dist && mkdir dist && cp -r manifest.json src dist/",
    "clean": "rm -rf dist"
  },
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "^0.0.266"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
